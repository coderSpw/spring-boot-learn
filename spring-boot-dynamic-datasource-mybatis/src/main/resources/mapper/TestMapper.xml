<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spw.dynamic.mapper.TestMapper" >

    <select id="findMaster" resultType="com.spw.dynamic.model.Sync">
        select id, name from sync
    </select>
    <select id="findSlave" resultType="com.spw.dynamic.model.Sync">
        select id, name from sync
    </select>
</mapper>
